{
  "name": "shoreline-template",
  "version": "1.0.0",
  "description": "Shoreline Pools — premium custom pool builder website template",
  "scripts": {
    "dev": "live-server --port=3000 --open=index.html"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
